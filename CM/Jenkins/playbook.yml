---
- hosts: all
  gather_facts: no
  
  roles:
    # - { name: Jenkins, become: yes, become_method: sudo }
    - { name: iTrust, become: yes, become_method: sudo }
    # - { name: CheckBoxIOBuild, become: yes, become_method: sudo }